# 1. The "dSYM" Rules (The ones that worked for you)
**/*.dSYM/
**/*.dSYM/**

# 2. The Specific "VS Code" & "Mac" Rules
# We use the double-star to make sure it hits every subfolder
**/.vscode/
**/.DS_Store
.vscode/
.DS_Store

# 3. The Mac Binary Trick (Keep this at the VERY bottom)
# This ignores compiled C++ programs with no extension
*
!*.*
!*/
